server:
  error:
    include-stacktrace: on_param

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/petme?useSSL=false&allowPublicKeyRetrieval=true&createDatabaseIfNotExist=true
    username: root
    password: root
  jpa:
    database: default
    show-sql: 'true'
    hibernate:
      ddl-auto: update
